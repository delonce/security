# Загрузка дашбордов из папки
apiVersion: 1
providers:
  - name: 'Security Services'
    orgId: 1
    folder: ''
    type: file
    disableDeletion: false
    updateIntervalSeconds: 30
    options:
      path: /etc/grafana/provisioning/dashboards/json
